/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{html,js,jsx,ts,tsx}'], // Tailwind가 클래스를 스캔할 파일 경로
  theme: {
    extend: {}, // 테마 확장 가능
  },
  plugins: [], // 추가 플러그인 설정 가능
};
